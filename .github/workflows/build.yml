name: Build

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run Neofetch
        run: |
          sudo apt-get update && sudo apt-get install -y neofetch
          neofetch
          
