name: Build

on: workflow_dispatch

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Install and Run Neofetch
        shell: pwsh
        run: |
          iwr -useb get.scoop.it | iex
          scoop install neofetch
          neofetch
          
