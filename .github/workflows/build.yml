name: Build

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install and Run Fastfetch
        run: |
          sudo apt update -y
          sudo apt install fastfetch -y
          fastfetch
