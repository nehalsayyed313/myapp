name: Build

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run Neofetch
        run: |
          git clone https://github.com/renatoathaydes/LogFX.git
          cd LogFX
          docker build -t app .
          
